# Minimal configuration for OpenAPI generation (docs profile)

# Disable DB connections and anything reactive/server-heavy
spring.main.web-application-type=reactive
spring.r2dbc.url=
spring.r2dbc.username=
spring.r2dbc.password=

# Dummy values for JWT or other required secrets
jwt.secret=dummy
jwt.expiration.ms=3600000